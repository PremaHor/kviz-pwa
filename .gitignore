node_modules
dist
# esbuild výstup pro Vercel (generuje `npm run build` / `npm run build:api`)
api/generate-quiz.js
*.local
.DS_Store

# Tajné klíče a lokální konfigurace — na GitHub patří jen .env.example (šablona bez hodnot)
.env
.env.*
!.env.example
